{% extends 'base.html.twig' %}

{% block title %}Deal{% endblock %}

{% block body %}
    <h1>Deal</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ deal.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_deal_index') }}">back to list</a>

    <a href="{{ path('app_deal_edit', {'id': deal.id}) }}">edit</a>

    {{ include('deal/_delete_form.html.twig') }}
{% endblock %}
