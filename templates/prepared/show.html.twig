{% extends 'base.html.twig' %}

{% block title %}Prepared{% endblock %}

{% block body %}
    <h1>Prepared</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prepared.id }}</td>
            </tr>
            <tr>
                <th>DeliveryQuantity</th>
                <td>{{ prepared.deliveryQuantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prepared_index') }}">back to list</a>

    <a href="{{ path('app_prepared_edit', {'id': prepared.id}) }}">edit</a>

    {{ include('prepared/_delete_form.html.twig') }}
{% endblock %}
