<form method="post" action="{{ path('app_buyer_delete', {'id': buyer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ buyer.id) }}">
    <button class="btn">Delete</button>
</form>
